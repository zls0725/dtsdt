# ============================================================
# dtsdt.com —— Hugo + PaperMod 配置（中英双语）
# 中文在根目录，英文在 /en/。改完执行 ./deploy.sh 上线。
# ============================================================

baseURL = "https://dtsdt.com/"
title = "DTSDT"
defaultContentLanguage = "zh-cn"
defaultContentLanguageInSubdir = false   # 中文不放进 /zh/，老 URL 才能对上
hasCJKLanguage = true                    # 中文按字计字数，阅读时长才准
timeZone = "Asia/Hong_Kong"
theme = "PaperMod"
summaryLength = 60
enableRobotsTXT = true
enableEmoji = true
enableGitInfo = true           # lastmod 取 git 提交时间，SEO 的"更新及时"

[pagination]
  pagerSize = 20

[frontmatter]
  lastmod = ["lastmod", ":git", "date", "publishDate"]
  date = ["date", "publishDate", "lastmod"]

[taxonomies]
  tag = "tags"
  category = "categories"
  series = "series"

[outputs]
  home = ["HTML", "RSS", "JSON"]
  section = ["HTML", "RSS"]
  taxonomy = ["HTML", "RSS"]
  term = ["HTML", "RSS"]

# ---------- 两种语言共用的外观 / 结构 ----------
[params]
  env = "production"
  author = "DTSDT"
  DateFormat = "2006-01-02"
  defaultTheme = "auto"
  disableThemeToggle = false
  mainSections = ["article"]
  ShowReadingTime = true
  ShowWordCount = true
  ShowToc = true
  TocOpen = false
  ShowBreadCrumbs = true
  ShowPostNavLinks = true
  ShowCodeCopyButtons = true
  ShowShareButtons = false
  ShowRssButtonInSectionTermList = true
  ShowFullTextinRSS = true
  ShowAllPagesInArchive = true
  disableSpecial1stPost = true
  disableScrollToTop = false
  hideFooter = false

  [params.assets]
    favicon = "/favicon.ico"
    disableFingerprinting = false

  [params.schema]
    publisherType = "Person"
    sameAs = ["https://github.com/zls0725"]

  [params.fuseOpts]
    isCaseSensitive = false
    shouldSort = true
    location = 0
    distance = 200
    threshold = 0.3
    minMatchCharLength = 2
    keys = ["title", "permalink", "summary", "content"]

# ---------- 语言 ----------
[languages]
  # ===== 中文（默认，根目录）=====
  [languages.zh-cn]
    locale = "zh-CN"
    title = "DTSDT"
    contentDir = "content/zh"
    weight = 1
    [languages.zh-cn.params]
      description = "DTSDT：海外资产配置一站式服务 —— 留学移民与教育规划、资产配置、保险理财、家族办公室。"
      keywords = ["dtsdt", "海外资产配置", "留学移民", "保险理财", "家族办公室"]
      [languages.zh-cn.params.homeInfoParams]
        Title = "DTSDT"
        Content = "海外资产配置一站式服务：留学移民与教育规划、资产配置、保险理财、家族办公室。"
      [languages.zh-cn.params.hub]
        name = "DTSDT"
        tagline = "海外资产配置一站式服务：留学移民与教育规划、资产配置、保险理财、家族办公室。"
    [[languages.zh-cn.menus.main]]
      name = "服务"
      url = "/services/"
      weight = 5
    [[languages.zh-cn.menus.main]]
      name = "文章"
      url = "/article/"
      weight = 10
    [[languages.zh-cn.menus.main]]
      name = "搜索"
      url = "/search/"
      weight = 40
    [[languages.zh-cn.menus.main]]
      name = "关于"
      url = "/about/"
      weight = 50

  # ===== 英文（/en/）=====
  [languages.en]
    locale = "en-US"
    title = "DTSDT"
    contentDir = "content/en"
    weight = 2
    [languages.en.params]
      description = "dtsdt's personal site: articles, notes and project logs."
      keywords = ["dtsdt", "blog", "notes"]
      [languages.en.params.homeInfoParams]
        Title = "dtsdt"
        Content = "Articles and notes. The site was rebuilt with Hugo: faster pages, complete SEO, simpler publishing."
      [languages.en.params.hub]
        name = "dtsdt"
        tagline = "My home on the web: posts, tools and projects here, plus where else to find me."
    [[languages.en.menus.main]]
      name = "Articles"
      url = "/en/article/"
      weight = 10
    [[languages.en.menus.main]]
      name = "Search"
      url = "/en/search/"
      weight = 40
    [[languages.en.menus.main]]
      name = "About"
      url = "/en/about/"
      weight = 50

# ---------- Markdown / 代码高亮 ----------
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.highlight]
    noClasses = false
    codeFences = true
    guessSyntax = true
    lineNos = false
    anchorLineNos = false

# ---------- 站点地图 ----------
[sitemap]
  changefreq = "weekly"
  priority = 0.5
  filename = "sitemap.xml"
